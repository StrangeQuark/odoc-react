# Vite does not receive API credentials or API origins at build time. The
# deployed static server publishes public/runtime-config.json instead.
#
# Use this file only for future, non-secret local Vite settings.
